import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function PricingPage() {
  return (
    <PlaceholderPage
      title="Pricing"
      description="Pricing table(s), plan comparisons, and FAQ/trust blocks."
    />
  );
}

